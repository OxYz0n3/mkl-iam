{
  "name": "mkl-iam",
  "private": true,
  "scripts": {
    "dev": "bun --parallel --filter '*' dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
